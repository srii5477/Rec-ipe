<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe Card</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="style.css" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap">
    <link href="card.css" rel="stylesheet">
</head>
<body>

    <% if(locals.card) { %>
        <div class="main">
            <ul class="cards">
              <li class="cards_item">
                <div class="card">
                  <div class="card_image" style="height: 400px; width: 600px;"><img src="<%= card.image_link %>" alt="<%= card.title %>" height="600px" width="100px"></div>
                  <div class="card_content">
                    <h1 class="card_title"><%= card.title %></h1>
                    <p> <h4> Ingredients: </h4> <%= card.ingredients %> </p>

                    <h4> Source of this recipe: </h4> <p><%= card.src %></p>
                    <h4> Health Labels: </h4> <p><%= card.healthLabels %></p>
                    <h4> Cuisine Type: </h4> <p><%= card.cuisine %></p>
                    <h4> Calories: </h4> <p><%= card.calories %></p>
                    <h4> Tags: </h4> <p><%= card.tags %></p>
                  </div>
                </div>
              </li>
          
            </ul>
          </div>
        <form action="/" method="GET">
            <h3> You've reached the end. </h3>
            <button type="submit" class="btn btn-outline-warning btn-lg"> Take me back. </button>
        </form>
    <% } else { %>
    <form action="/" method="GET">
        <h3> Unknown error! </h3>
        <button type="submit" class="btn btn-outline-warning btn-lg"> Take me back. </button>
    </form>
    <% } %>
    
</body>
</html>